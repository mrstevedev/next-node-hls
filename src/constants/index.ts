/**
 * API ROUTES
 */

export enum ROUTE {
  SUBSCRIBE = "/subscribe",
}

/**
 * STATUS
 * Newsletter
 */

export enum STATUS {
  STATUS_CONFIRMED = "confirmed",
  STATUS_UNSUBSCRIBED = "unsubscribed",
}
